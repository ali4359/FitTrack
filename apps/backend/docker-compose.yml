services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fittrack
    ports:
      - "5432:5432"
    volumes:
      - fittrack_pg:/var/lib/postgresql/data

volumes:
  fittrack_pg:

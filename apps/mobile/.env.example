# Root of the Go backend (no trailing slash, no /api). The API client appends /api.
# Simulator / web: http://localhost:8080
# Physical device: use your machine's LAN IP, e.g. http://192.168.1.20:8080
API_BASE_URL=http://localhost:8080
